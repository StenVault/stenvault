/**
 * Public Send — Password constants (canonical).
 * Shared between frontend validation and backend Zod schema.
 */

/** Minimum password length for password-protected send sessions. */
export const SEND_PASSWORD_MIN_LENGTH = 6;
